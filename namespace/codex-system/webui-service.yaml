apiVersion: v1
kind: Service
metadata:
  name: codex-webui
  namespace: codex-system
spec:
  selector:
    app: codex-webui
  ports:
    - name: http
      port: 8899
      targetPort: http
  type: ClusterIP
