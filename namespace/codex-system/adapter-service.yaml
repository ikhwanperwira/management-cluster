apiVersion: v1
kind: Service
metadata:
  name: codex-adapter
  namespace: codex-system
spec:
  selector:
    app: codex-adapter
  ports:
    - name: http
      port: 9000
      targetPort: http
  type: ClusterIP
